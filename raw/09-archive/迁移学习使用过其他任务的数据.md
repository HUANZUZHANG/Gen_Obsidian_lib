

---
title: "(新版中英!) B站公认最好的【吴恩达机器学习】教程！附课件代码 Machine Learning Specialization"
url: "https://www.bilibili.com/video/BV16jyuBBEom/?spm_id_from=333.1391.0.0&p=83&vd_source=e99322ea5c5f5f6f8702b73625802727"
bvid: "BV16jyuBBEom"
cid: "34149106140"
author: "吴恩达机器学习"
upload_date: "2025-11-21"
subtitle_lang: "中文（中国）"
created: "2026-05-14"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115585505560339&bvid=BV16jyuBBEom&cid=34149106140&page=83&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

deep learning.ai
相信很多同学认识吴恩达大佬，在这门课上( machine earning specialization)你将自己编程实现机器学习。数百万人参加了这门课程的早期版本许多学习者，最终建立了令人兴奋的机器学习系统，甚至在人工智能AI领域获得非常成功的职业生涯。欢迎你的到来
(配套课件、笔记已整理，求三连！ )

## 字幕

`00:02` 对于一款数据量不多的应用 
For an application where you don't have that much data,
`00:05` 迁移学习是一种很棒的技术 它让你可以使用来自不同任务的数据来帮助你的应用 
transfer learning is a wonderful technique that lets you use data from a different task to help on your application.
`00:13` 这是我经常使用的技术之一 
This is one of those techniques that I use very frequently.
`00:16` 让我们来看看迁移学习是如何运作的 
Let's take a look at how transfer learning works.
`00:19` 这就是迁移学习的工作原理 
So here's how transfer learning works.
`00:22` 假设你想识别0到9的手写数字
Let's say you want to recognize the handwritten digits from 0 through 9,
`00:28` 但你没有太多这些手写数字的标签数据 
but you don't have that much label data of these handwritten digits.
`00:32` 你可以这样做 
Here's what you can do.
`00:34` 假设你找到一个包含100万张猫图片的大型数据集 
Say you find a very large data set of 1 million images of pictures of cats,
`00:41` 狗，汽车，人，等等
dogs, cars, people, and so on.
`00:44` 一千个类别 
A thousand classes.
`00:45` 然后你可以先在这个包含一百万张图片和一千个不同类别的大型数据集上训练一个神经网络
You can then start by training a neural network on this large data set of a million images with a thousand different
`00:52` 类别并训练算法输入图像x 并学习识别这1,000个类别中的任意一个
classes and train the algorithm to take as input an image x and learn to recognize any of these 1,000
`00:59` 不同的类别 
different classes.
`01:01` 在这个过程中 你最终学会了神经网络第一层的参数 
In this process, you end up learning parameters for the first layer of the neural network,
`01:06` 第二层的w1, b1
w1, b1, for the second layer,
`01:08` w2, b2, 等等 w3,
w2, b2, and so on, w3,
`01:11` b3, w4, b4 以及w5, b5的参数 
b3, w4, b4, and w5, b5 for the upper layer.
`01:17` 应用迁移学习
To apply transfer learning,
`01:19` 你所做的是复制这个神经网络 并保留参数w1 
what you do is then make a copy of this neural network where you would keep the parameters w1,
`01:26` b1, w2, b2, w3, b3, 和 w4,
b1, w2, b2, w3, b3, and w4,
`01:31` b4 
b4.
`01:32` 但对于最后一层
But for the last layer,
`01:34` 你将去掉输出层 并用一个更小的输出层替换它 只有10个输出而不是1000个 
you would eliminate the output layer and replace it with a much smaller output layer with just 10 rather than 1,000
`01:44` 输出单元 
output units.
`01:47` 这10个输出单元将对应于类别0 
And these 10 output units will correspond to the classes 0,
`01:50` 1 到 9 你希望你的神经网络识别的类别 
1, through 9 that you want your neural network to recognize.
`01:54` 注意参数w5,
Notice that the parameters w5,
`01:56` b5 它们不能被复制 因为这层的维度已经改变 
b5, they can't be copied over because the dimension of this layer has changed.
`02:02` 所以你需要设计新的参数w5
So you need to come up with new parameters w5,
`02:06` b5 你需要从头开始训练 而不是直接从之前的神经网络中复制
b5 that you need to train from scratch rather than just copy it from the previous neural network.
`02:14` 在迁移学习中
And in transfer learning,
`02:16` 你可以做的是使用前四层的参数
what you can do is use the parameters from the first four layers,
`02:22` 实际上是所有层 除了最后的输出层 作为参数的起点 然后运行一个优化
really all the layers except the final output layer as a starting point for the parameters and then run an optimization
`02:28` 算法 比如梯度下降或Adam优化算法 参数初始化使用
algorithm such as gradient descent or the Adam optimization algorithm with the parameters initialized using the values
`02:35` 这个神经网络上面的值
from this neural network up on top.
`02:38` 详细来说 你有两种选择来训练这个神经网络的参数
So in detail, there are two options for how you can train this neural network's parameters.
`02:44` 选项一是你只训练输出层的参数
Option one is you only train the output layer's parameters.
`02:48` 所以你会使用参数w1,
So you would take the parameters w1,
`02:51` b1, w2, b2 到 w4,
b1, w2, b2 through w4,
`02:53` b4 作为上面的值 并保持它们固定 不用去改变它们 使用像
b4 as the values from on top and just hold them fixed and don't even bother to change them and use an algorithm like
`03:00` 随机梯度下降或Adam优化算法 只更新w5,
stochastic gradient descent or the Adam optimization algorithm to only update w5,
`03:05` b5 以降低成本函数
b5 to lower the cost function,
`03:08` 你通常用来学习识别这些数字0到9的成本函数 从一个小的训练集这些
the usual cost function that you use for learning to recognize these digits 0 to 9 from a small training set of these
`03:14` 数字0到9
digits 0 to 9.
`03:17` 所以这是选项一
So that's option one.
`03:18` 选项二是训练网络中的所有参数 包括w1,
Option two would be to train all the parameters in the network including w1,
`03:23` b1, w2, b2 一直到w5,
b1, w2, b2 all the way through w5,
`03:25` b5 但前四层的参数将使用你在上面训练的值进行初始化
b5 but the first four layer's parameters would be initialized using the values that you had trained on top.
`03:33` 如果你有一个非常非常小的训练集 那么选项一可能会更好一些 但如果你有一个训练
If you have a very, very small training set then option one might work a little bit better but if you have a training
`03:41` 集稍微大一点 那么选项二可能会更好一些 
set that's a little bit larger then option two might work a little bit better.
`03:46` 这个算法被称为迁移学习 因为直觉上通过学习识别猫 
This algorithm is called transfer learning because the intuition is by learning to recognize cats,
`03:53` 狗 车 人等等 它有望为前几层学习到一些合理的参数集 
dogs, cars, people and so on it will hopefully have learned some plausible sets of parameters for the earlier layers for
`04:01` 处理图像输入 然后通过将这些参数转移到新的神经网络 新的神经网络
processing image inputs and then by transferring these parameters to the new neural network the new neural network
`04:08` 开始时参数处于更好的位置 因此只需稍加学习 希望它能
starts off with the parameters in a much better place so that with just a little bit of further learning hopefully it
`04:15` 最终得到一个相当好的模型 
can end up at a pretty good model.
`04:18` 这两个步骤首先在大型数据集上训练 然后在较小的数据集上进一步调整参数 叫做
These two steps of first training on a large data set and then tuning the parameters further on a smaller data set go by
`04:26` 在大型数据集上训练神经网络的步骤称为有监督预训练
the name of supervised pre-training for the step on top that's when you train the neural network on a very large data
`04:34` 比如说一百万张与实际任务不完全相关的图片集 然后第二步称为微调 你可以采取
set of say a million images of not quite the related tasks and then the second step is called fine tuning where you take
`04:42` 你从有监督预训练中初始化或获得的参数 然后进一步运行梯度下降以
the parameters that you had initialized or gotten from supervised pre-training and then run gradient descent further to
`04:49` 微调权重以适应你可能有的手写数字识别的特定应用 
fine-tune the weights to suit the specific application of handwritten digit recognition that you may have.
`04:56` 因此 如果你有一个小数据集 即使只有几十或者几百 或者几千 或者只有成千上万张图片
And so if you have a small data set even tens or hundreds or thousands or just tens of thousands of images of the
`05:03` 手写数字能够从这百万张不完全相关的任务图片中学习 实际上可以帮助你的学习
handwritten digits able to learn from this million images of a not quite related task can actually help your learning
`05:11` 提升很多 
performance a lot.
`05:13` 迁移学习的一个好处是 你不需要亲自进行监督
One nice thing about transfer learning as well is maybe you don't need to be the one to carry out supervised
`05:21` 预训练 
pre-training.
`05:22` 对于许多神经网络 已经有研究人员在大型数据集上训练了神经网络
For a lot of neural networks there will already be researchers that have already trained a neural network on a large
`05:28` 图像 并且他们会将训练好的神经网络发布在互联网上 任何人都可以免费下载和使用
image and will have posted they trained neural networks on the internet freely licensed for anyone to download and use.
`05:38` 这意味着你不需要亲自进行第一步 你可以直接下载一个神经网络
And what that means is rather than carrying out the first step yourself you can just download a neural network that
`05:44` 其他人可能花了几周时间训练 然后用你自己的输出层替换输出层 并进行
someone else may have spent weeks training and then replace the output layer with your own output layer and carry out
`05:51` 选择选项一或选项二来微调一个已经由他人进行监督
either option one or option two to fine-tune a neural network that someone else has already carried out supervised
`05:58` 预训练的神经网络 只需稍加微调 就能快速获得一个在
pre-training on and just do a little bit of fine-tuning to quickly be able to get a neural network that performs well on
`06:04` 你的任务上表现良好的神经网络
your task.
`06:06` 下载其他人已经训练好的 并且免费提供的预训练模型 是一种通过
Downloading a pre-trained model that someone else has trained and provided for free is one of those techniques where by
`06:13` 在机器学习社区中建立在彼此的工作基础上 我们可以通过其他
building on each other's work in the machine learning community we can all get much better results by the generosity of
`06:20` 研究人员慷慨预训练并发布的神经网络 在网上分享 得到更好结果的技术之一
other researchers that have pre-trained and posted their neural networks online.
`06:24` 但为什么迁移学习能起作用呢？
But why does transfer learning even work?
`06:27` 你怎么可能使用识别猫 
How could you possibly take parameters obtained by recognizing cats,
`06:31` 狗 汽车和人的参数来帮助你识别如手写数字这样完全不同的东西呢？
dogs, cars, and people and use that to help you recognize something as different as handwritten digits?
`06:38` 这里有一些直觉解释
Here's some intuition behind it.
`06:41` 如果你正在训练一个神经网络来检测
If you are training a neural network to detect,
`06:45` 比如说 从图像中识别不同的物体
say, different objects from images,
`06:49` 那么神经网络的第一层可能会学习在图像中检测边缘
then the first layer of a neural network may learn to detect edges in the image.
`06:55` 我们认为这些是图像中的一些低级特征
We think of these as somewhat low-level features in the image,
`06:58` 也就是检测边缘
which is to detect edges.
`07:00` 这些方块中的每一个都可视化了一个神经元学会检测的内容
Each of these squares is a visualization of what a single neuron has learned to detect,
`07:05` 即它学会了将像素组合在一起以在图像中找到边缘
has learned to group together pixels to find edges in an image.
`07:10` 神经网络的下一层则学习将边缘组合在一起以检测角点
The next layer of the neural network then learns to group together edges to detect corners.
`07:16` 所以这些都是一个神经元可能学会检测的内容的可视化
And so each of these is a visualization of what one neuron may have learned to detect,
`07:22` 它学会了检测一些像角点这样的小简单形状
has learned to detect little simple shapes like corner-like shapes like this.
`07:27` 神经网络的下一层可能会学习检测一些更复杂但仍然通用的形状
And the next layer of the neural network may have learned to detect somewhat more complex but still generic shapes like
`07:33` 比如基本曲线或这些小形状
basic curves or small little shapes like these.
`07:37` 这就是为什么通过学习检测许多不同的图像
And that's why by learning on detecting lots of different images,
`07:43` 你在教神经网络检测边缘
you're teaching the neural network to detect edges,
`07:45` 角点和基本形状
corners, and basic shapes.
`07:47` 这就是为什么训练一个神经网络来检测猫 狗 汽车和人等多种多样的事物
And that's why by training a neural network to detect things as diverse as cats and dogs and cars and people,
`07:53` 是在帮助它学习检测这些相当通用的图像特征 如发现边缘
you're helping it to learn to detect these pretty generic features of images and finding edges,
`08:01` 角点 曲线 基本形状
corners, curves, basic shapes.
`08:03` 这对许多其他计算机视觉任务如识别手写数字都是有用的
This is useful for many other computer vision tasks such as recognizing handwritten digits.
`08:10` 预训练的一个限制是 图像类型x必须与预训练和
One restriction of pre-training though is that the image type x has to be the same for the pre-training and the
`08:18` 微调步骤
fine-tuning steps.
`08:19` 所以如果你最终想解决的任务是一个计算机视觉任务
So if the final task you want to solve is a computer vision task,
`08:24` 那么预训练步骤也必须是一个在相同类型输入上训练的神经网络
then the pre-training step also has to be a neural network trained on the same type of input,
`08:29` 即所需尺寸的图像
namely an image of the desired dimensions.
`08:33` 相反 如果你的目标是构建一个处理音频的语音识别系统
Conversely, if your goal is to build a speech recognition system to process audio,
`08:38` 那么在图像上预训练的神经网络可能对音频没有多大帮助
then a neural network pre-trained on images probably won't do much good on audio.
`08:43` 相反 你需要一个在音频数据上预训练的神经网络 然后在自己的音频数据集上进行微调
Instead, you want a neural network pre-trained on audio data that you then fine-tune on your own audio dataset.
`08:49` 其他类型的应用程序也一样
And the same for other types of applications.
`08:52` 你可以在文本数据上预训练一个神经网络
You can pre-train a neural network on text data.
`08:55` 如果你的应用程序具有相同文本数据输入x
And if your application has the same feature input x of text data,
`08:59` 那么你就可以在自己的数据上微调那个神经网络 
then you can fine-tune that neural network on your own data.
`09:02` 总的来说 这是迁移学习的两个步骤 
To summarize, these are the two steps for transfer learning.
`09:07` 第一步是下载一个在具有相同输入的大型数据集上预训练的参数神经网络
Step one is download a neural network with parameters that have been pre-trained on a large dataset with the same input
`09:15` 与您的应用程序类型相同
type as your application.
`09:17` 这个输入类型可以是图像
And that input type could be images,
`09:19` 音频 文本或其他类型 
audio, text, or something else.
`09:21` 或者你不想下载神经网络
Or if you don't want to download a neural network,
`09:23` 也许你可以自己训练一个 
maybe you can train your own.
`09:25` 但在实际操作中 如果你使用的是图像
But in practice, if you're using images,
`09:28` 可以说 下载别人预训练的神经网络更为常见 
say it's much more common to download someone else's pre-trained neural network.
`09:32` 然后在自己的数据上进一步训练或微调网络 
Then further train or fine-tune the network on your own data.
`09:39` 我发现 如果你能获得一个在大型数据集上预训练的神经网络
And I found that if you can get a neural network pre-trained on a large dataset,
`09:43` 比如一百万张图像
say a million images,
`09:44` 那么有时候你可以使用一个小得多的数据集 
then sometimes you can use a much smaller dataset,
`09:48` 也许只需要一千张图像 
maybe a thousand images,
`09:49` 甚至更少地微调神经网络 以自己的数据来获得不错的结果 
maybe even smaller, to fine-tune the neural network on your own data and get pretty good results.
`09:56` 使用这种技术时 我有时在只有 50 张图像的数据上训练神经网络 效果也很好 
And I've sometimes trained neural networks on as few as 50 images that work quite well using this technique when it has
`10:03` 已经在一个更大规模的数据集上预训练过了 
already been pre-trained on a much larger dataset.
`10:07` 这种技术并不是万能的 
This technique isn't panacea.
`10:09` 并不是每个应用都能仅依靠50张图像来运行
You can't get every application to work just on 50 images,
`10:12` 但当你用于应用的数据集不够大时 这种方法确实能帮上很多忙 
but it does help a lot when the dataset you have for your application isn't that large.
`10:18` 顺便说一下 如果你在新闻中听说过像GPT-3或BERT这样的先进技术 或者已经预训练的神经网络上的技术
And by the way, if you've heard of advanced techniques in the news like GPT-3 or BERT or neural networks pre-trained on
`10:26` ImageNets 这些其实就是别人已经在一个非常大的图像数据集上预训练的神经网络的例子
ImageNets, those are actually examples of neural networks that someone else has pre-trained on a very large image
`10:34` 数据集或文本数据集然后可以在其他应用上进行微调 
dataset or text dataset that can then be fine-tuned on other applications.
`10:39` 如果你没听说过 GPT-3 或 BERT 或 ImageNet
If you haven't heard of GPT-3 or BERT or ImageNet,
`10:41` 不用担心 
don't worry about it,
`10:42` 但如果你听说过 这些都是机器学习文献中 预训练成功应用的例子
but if you have, those have been successful applications of pre-training in the machine learning literature.
`10:48` 如果你没听说过 GPT-3 或 BERT 或 ImageNet
If you haven't heard of GPT-3 or BERT or ImageNet,
`10:51` 不用担心
don't worry about it,
`10:51` 但如果你听说过 这些都是机器学习文献中迁移学习成功应用的例子
but if you have, those have been successful applications of transfer learning in the machine learning literature.
`10:57` 我喜欢迁移学习的原因之一是 这是机器学习社区的一种方式
One of the things I like about transfer learning is it's been one of the ways that the machine learning community has
`11:03` 共享想法和代码 甚至参数
shared ideas and code and even parameters with each other.
`11:07` 这要感谢那些预训练了大型神经网络并将参数发布在互联网上的研究人员
Because thanks to the researchers that have pre-trained large neural networks and posted the parameters on the internet
`11:13` 任何人都可以自由下载和使用
freely for anyone else to download and use,
`11:16` 这使得任何人都可以使用他们预训练的模型 并在规模较小的数据集上进行微调 
this empowers anyone to take models they've pre-trained to fine-tune on potentially a much smaller dataset.
`11:23` 在机器学习中 我们经常以彼此的工作为基础 这种开放的思想交流 
In machine learning, all of us end up often building on the work of each other and that open sharing of ideas,
`11:31` 代码 训练参数 是机器学习社区的其中一种方式
of code, of trained parameters is one of the ways that the machine learning community,
`11:36` 我们所有人共同努力
all of us collectively,
`11:38` 能够做出远超过任何个人单独完成的工作 
manage to do much better work than any single person by themselves can.
`11:42` 因此 我希望你加入机器学习社区后 将来能够找到一种方式回馈社区 
And so I hope that you joining the machine learning community will someday maybe find a way to contribute back to this
`11:48` 社区 
community as well.
`11:49` 预训练部分就讲到这里 
So that's it for pre-training.
`11:53` 希望你觉得这个技巧有用 下一段视频中 
I hope you find this technique useful and in the next video,
`11:57` 我想和你分享一下关于机器学习项目完整周期的一些想法 
I'd like to share with you some thoughts on the full cycle of a machine learning project.
`12:03` 构建机器学习系统时 
So when building a machine learning system,
`12:05` 有哪些步骤值得考虑？
what are all the steps that are worth thinking about?
`12:08` 我们在下一段视频中看看这个问题 
Let's take a look at that in the next video.